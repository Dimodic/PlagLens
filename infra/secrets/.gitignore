# All secret files in this directory are gitignored by default.
# Only this .gitignore itself is committed so the directory exists in the
# checkout for the docker-compose volume mount.
*
!.gitignore
!README.md

[pytest]
asyncio_mode = auto
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    smoke: smoke tests that verify the stack is up
    auth: tests that exercise the auth/RBAC flow

/**
 * Legacy header — superseded by `<Header />` in `components/shell/Header.tsx`.
 * Kept as a thin re-export so any residual imports keep resolving.
 */
export { Header } from '@/components/shell/Header';

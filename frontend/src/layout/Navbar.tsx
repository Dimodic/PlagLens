/**
 * Legacy sidebar — superseded by `<Sidebar />` in
 * `components/shell/Sidebar.tsx`. Re-exported under its old name so any
 * residual import keeps resolving.
 */
export { Sidebar as Navbar } from '@/components/shell/Sidebar';
